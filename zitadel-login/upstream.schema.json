{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Upstream tracking pin",
  "type": "object",
  "required": ["repo", "ref", "vendorDir"],
  "properties": {
    "repo": { "type": "string", "description": "Upstream git remote URL." },
    "ref": {
      "type": "string",
      "description": "Branch, tag, or commit ref to track."
    },
    "subdir": {
      "type": "string",
      "description": "Subdirectory within the upstream repo to vendor ('.' for the whole repo)."
    },
    "vendorDir": {
      "type": "string",
      "description": "Local directory (relative to this app) where upstream is materialized."
    },
    "license": { "type": "string" },
    "notes": { "type": "string" },
    "pinnedCommit": {
      "type": ["string", "null"],
      "description": "Exact commit SHA last resolved for `ref`."
    },
    "lastVendoredAt": {
      "type": ["string", "null"],
      "description": "ISO timestamp of the last successful vendor run."
    }
  }
}
